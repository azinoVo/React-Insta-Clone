import React from 'react';
import CommentSection from '../CommentSection/CommentSection';

// Function or Class Here
// Gets each instance of the array as objects props - props.objectFromArray
//  from Parent App, <PostContainer arrayofObjects={objectOfDummyData} />
// EX: return <div>{props.memos.map(memo => <Quote quote={memo} />)}</div>;

const PostContainer = props => {
    return (
        <div>

        </div>
    );
}


export default PostContainer;